#!/usr/bin/env python3"""پیکربندی اصلی سیستم - Financial Analyzer ProDeveloper: Seyed Aladdin Mousavi Jashni"""
import osfrom datetime import timedelta
class Config:    """کلاس پیکربندی اصلی سیستم"""        # اطلاعات پایه    APP_NAME = "Financial Analyzer Pro"    VERSION = "1.0.0"    DEVELOPER = "Seyed Aladdin Mousavi Jashni"    SUPPORT_EMAIL = "support@financial-analyzer.pro"        # تنظیمات سرور    HOST = "0.0.0.0"    PORT = 8000    DEBUG = True        # تنظیمات امنیتی    SECRET_KEY = os.getenv("SECRET_KEY", "dev-secret-key-change-in-production")    JWT_ALGORITHM = "HS256"    JWT_EXPIRATION = timedelta(hours=24)        # تنظیمات API    API_RATE_LIMIT = 100 # درخواست در دقیقه    REQUEST_TIMEOUT = 30 # ثانیه        # تنظیمات تحلیل بازار    CRISIS_MONITORING = {        "UPDATE_INTERVAL": 300, # 5 minutes        "ALERT_THRESHOLD": 0.7,        "MAX_INDICATORS": 50    }        MONTE_CARLO = {        "SIMULATION_COUNT": 10000,        "CONFIDENCE_LEVELS": [0.90, 0.95, 0.99],        "TIME_HORIZON": 252 # روزهای معاملاتی در سال    }        # تنظیمات سیگنال‌دهی    SIGNAL_SETTINGS = {        "INTRADAY_TIMEFRAME": "15m",        "SWING_TIMEFRAME": "4h",        "POSITION_TIMEFRAME": "1d",        "MIN_CONFIDENCE": 0.75    }        # اینسترومنت‌های تحت نظارت    MONITORED_INSTRUMENTS = {        "FOREX": [            "EUR/USD", "GBP/USD", "USD/JPY", "USD/CHF",            "AUD/USD", "USD/CAD", "NZD/USD", "EUR/GBP"        ],        "CRYPTO": [            "BTC/USD", "ETH/USD", "XRP/USD", "ADA/USD",            "DOT/USD", "LTC/USD", "LINK/USD", "BCH/USD"        ],        "INDICES": [            "SPX", "NDX", "DJI", "FTSE", "DAX",            "CAC", "NIKKEI", "ASX", "HSI", "SSE"        ],        "METALS": [            "XAU/USD", "XAG/USD", "XPT/USD", "XPD/USD"        ]    }        # تنظیمات اتصال API    API_CONFIG = {        "ALPHA_VANTAGE": {            "BASE_URL": "https://www.alphavantage.co/query",            "RATE_LIMIT": 5 # requests per minute        },        "TWELVE_DATA": {            "BASE_URL": "https://api.twelvedata.com",            "RATE_LIMIT": 8        },        "OANDA": {            "BASE_URL": "https://api-fxtrade.oanda.com",            "RATE_LIMIT": 60        }    }
# نمونه پیکربندی توسعهclass DevelopmentConfig(Config):    DEBUG = True    TESTING = True    DATABASE_URL = "sqlite:///./dev.db"
# نمونه پیکربندی تولیدclass ProductionConfig(Config):    DEBUG = False    TESTING = False    DATABASE_URL = os.getenv("DATABASE_URL")
# انتخاب پیکربندی بر اساس محیطif os.getenv("ENVIRONMENT") == "production":    config = ProductionConfig()else:    config = DevelopmentConfig()
